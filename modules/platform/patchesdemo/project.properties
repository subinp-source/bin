# -----------------------------------------------------------------------
# Copyright (c) 2020 SAP SE or an SAP affiliate company. All rights reserved.
# -----------------------------------------------------------------------

# you can put key/value pairs here.
# Use Config.getParameter(..) to retrieve the values during runtime.

patchesdemo.key=value

# Specifies the location of the spring context file putted automatically to the global platform application context.
patchesdemo.application-context=patchesdemo-spring.xml

# project specific part of file path to place where impexes are placed. 
# it should start from / and is used for subfolder of resource folder that is usually name of project extension.  
patches.extension.folder.name=/patchesdemo

patches.jenkinsBuild=exampleJenkinsBuild
patches.gitHashCode=exampleGitHashCode

# Switch the patch 3x0 by default as it is containing broken actions which cause errors.
# Should be switched on if you wish to demo error handling
patches.minor_error_patch.notSelected=true
patches.major_error_patch.notSelected=true

#Extension list for which project data should be loaded during running ant updatesystem script
update.executeProjectData.extensionName.list=patchesdemo